# LEARNING_TASKS.md — План улучшений учебной программы Data Analyst

> Аудит проведён 2026-06-10. Источник: `learning-plan/data/{plan,tasks,projects,career,practiceContent}.json` и `cases/`.
> Принцип: данные и материалы **не изменялись**. Ниже — только бэклог улучшений с точными адресами файлов.

Легенда приоритетов: **Critical** (блокирует трудоустройство) · **High** (сильно влияет на найм/собес) · **Medium** (повышает качество) · **Low** (полировка).

---

## P0 — CRITICAL

> Примечание: достройку дней Месяцев 2–7 владелец плана выполняет самостоятельно — отдельным пунктом не отслеживается.

### ~~LT-02. Усилить продуктовую и бизнес-аналитику~~ ✓ ВЫПОЛНЕНО 2026-06-11
- **Что добавить:** минимум +12 задач в Месяц 6: North Star метрика, AARRR/pirate-метрики, выбор и дерево метрик, активация vs retention, сезонность vs эффект релиза, разбор «вырос трафик — не выросла выручка», приоритизация (ICE/RICE), формулировка продуктовой гипотезы, sizing эффекта фичи, decomposition метрики, guardrail-метрики, unit-economics (CAC, LTV, payback, маржа).
- **Куда:** `data/tasks.json` → `tasks[]` (расширить month=6 за счёт перераспределения, range product-banking сейчас [93,96]); `data/plan.json` → Месяц 6 недели.
- **Файлы менять:** `tasks.json`, `practiceContent.json` (добавить items с тем же schema), `plan.json`.
- **Кейсы:** добавить 3–4 кейса в `cases/` типа `hypothesis-*`/`detective-*` на продуктовые сценарии.
- **Датасеты:** событийная таблица `events(user_id, event, ts)` + `users(signup_date, channel, plan)` для воронки/retention/LTV.

### ~~LT-03. Добавить блок «AI-инструменты для аналитика» (сейчас ~0 упоминаний)~~ ✓ ВЫПОЛНЕНО 2026-06-11
- **Что добавить:** сквозной мини-трек (2–3 дня + рекуррентно): LLM как ассистент для SQL/pandas/DAX, написание и проверка промптов для генерации запросов, использование AI для EDA-черновика и объяснения данных, **критическая проверка галлюцинаций**, AI для подготовки к собесу (mock-вопросы), границы (нельзя слепо доверять цифрам, нельзя сливать чувствительные данные).
- **Куда:** новая неделя/дни в Месяце 5 (Python) и Месяце 7; рекуррентная заметка в `dailyTemplate`.
- **Файлы менять:** `plan.json` (`dailyTemplate`, месяцы 5 и 7), `tasks.json` (+3–4 задачи, новый skill `ai-tools`), `practiceContent.json`.
- **Практика:** «сгенерируй SQL через AI → найди ошибку в его выводе руками», «опиши датасет с AI и проверь 3 факта».
- **Кейсы:** добавить `detective-*` где AI дал правдоподобный, но неверный вывод — задача найти ошибку.

---

## P1 — HIGH

### ~~LT-04. Вынести коммуникации/презентации/стейкхолдеров из Месяца 7 в сквозной навык~~ ✓ ВЫПОЛНЕНО 2026-06-15
- **Что добавить:** правило «каждый якорный проект → 1 слайд + 30-сек устный вывод руководителю»; шаблон executive summary; работа с возражением «данные против гипотезы бизнеса».
- **Куда:** `plan.json` `rules[]` и `dailyTemplate` (блок «journal» расширить до «вывод стейкхолдеру»); задачи в месяцах 3, 5, 6, а не только 7.
- **Файлы:** `plan.json`, `tasks.json`, `career.json` (`productCaseQuestions` уже хороши — связать с практикой).

### ~~LT-05. Углубить статистику и A/B (сейчас 13 задач, в основном теория)~~ ✓ ВЫПОЛНЕНО 2026-06-15
- **Что добавить:** практический A/B end-to-end на датасете (дизайн → размер выборки → расчёт → интерпретация → решение), ошибки I/II рода на данных, выбор теста (t-test/χ²/пропорции), мощность, проблема подглядывания (peeking), множественные сравнения, нормальность/непараметрика.
- **Куда:** `plan.json` Месяц 4 недели 3–4; `tasks.json` range statistics [66,78] расширить.
- **Датасеты:** `ab_test_events.csv` (group, converted, revenue) с заранее заложенным эффектом и ловушкой (Симпсон/неравные группы).
- **Кейсы:** уже есть `hypothesis-*` — связать явно с Месяцем 4.

### LT-06. Сбалансировать Excel (Месяц 1 + 20 доп.задач) vs глубину Python/SQL
- **Проблема:** Excel перевешен (15 задач + 20 supplemental = 35 пунктов на 1 инструмент), Python всего 14 задач и заканчивается на EDA.
- **Что добавить в Python:** работа с датами/таймзонами, apply/lambda, объединение многих файлов (есть task-092 — расширить), базовый автоотчёт в функцию/скрипт, чтение из БД (`pd.read_sql`), экспорт в Excel/CSV для бизнеса.
- **Куда:** `plan.json` Месяц 5, `tasks.json` range [79,92], `practiceContent.json`.
- **Перебалансировка:** часть Excel-supplemental пометить как опциональные (`optional: true` в `tasks.json`), не сокращая данные.

### LT-07. Добавить современный BI-стек и облако (рынок 2026 / remote)
- **Что добавить (обзорно, не глубоко):** Google Sheets как альтернатива Excel (remote-команды), один из Tableau/Metabase/Looker Studio (бесплатный Looker Studio — приоритет), понятие хранилища/BigQuery, что такое dbt и слой витрин (1 ознакомительный день).
- **Куда:** `plan.json` Месяц 3 (BI) неделя 4 — ознакомительный день; `tasks.json` +2 задачи skill `bi-modern`.
- **Зачем:** remote-вакансии и KZ-финтех всё чаще требуют облачный SQL и не-Power BI инструменты.

---

## P2 — MEDIUM

### LT-08. Добавить навык дизайна метрик и data quality как отдельную тему
- **Что:** чек-лист качества данных (полнота, дубли, типы, аномалии, сверка с источником) как именованный навык; определение метрики (числитель/знаменатель/окно/фильтры).
- **Куда:** `plan.json` Месяц 1 нед.2 и Месяц 2; усилить `qualityChecklist` в `tasks.json`/`practiceContent.json` (уже частично есть в practice items — поднять в явный навык).

### LT-09. Связать `cases/` тренажёра с днями плана явными ссылками
- **Что:** в каждый день `plan.json` добавить `caseRefs: [...]` на конкретные `cases/*.json`, чтобы практика тренажёра привязывалась к расписанию (сейчас связь только через `trainerModuleMap` на уровне навыка).
- **Файлы:** `plan.json`, `cases/index.json`.

### LT-10. Добавить SQL-темы продакшн-уровня
- **Что:** индексы и чтение `EXPLAIN` (task-050 «оптимизация» есть — раскрыть), оконные рамки (ROWS/RANGE BETWEEN), date spine для непрерывных периодов, обработка NULL в JOIN, грануляция/дубли при джойнах.
- **Куда:** `plan.json` Месяц 2 нед.4; `tasks.json` range [16,50].

### LT-11. Расширить портфолио-кейсы метриками влияния
- **Что:** в `projects.json` каждый проект → обязательное поле «бизнес-эффект в цифрах» и «решение, которое приняли бы по итогам».
- **Файлы:** `projects.json` (`deliverables` → добавить пункт impact), `plan.json` `portfolio`.

---

## P3 — LOW

### LT-12. Английский B1–B2 как явный микро-трек для remote
- **Что:** 15 мин/день чтение англ. вакансий/доков, словарь терминов; цель из `career.json` (remote требует B1–B2) не подкреплена практикой.
- **Файлы:** `plan.json` `dailyTemplate` (опц. блок), `career.json`.

### LT-13. Добавить «дашборд-сторителлинг» и единый визуальный стандарт
- **Что:** правила подписей осей, выбор графика под задачу (task-061/типы графиков есть — оформить в reusable чек-лист), цветовой акцент на инсайт.
- **Файлы:** `plan.json` Месяц 3.

### LT-14. Версионирование и воспроизводимость раньше
- **Что:** Git вводится верно с Дня 1, но коммиты как навык — только Месяц 7. Добавить «коммить проект каждую неделю» в `rules`.
- **Файлы:** `plan.json` `rules[]`.

---

## Сводка адресов файлов

| Файл | Что трогаем |
|---|---|
| `data/plan.json` | AI-трек (LT-03), коммуникации/rules (LT-04, LT-14), статистика (LT-05), Python (LT-06), BI-стек (LT-07), caseRefs (LT-09), SQL (LT-10) |
| `data/tasks.json` | +продукт (LT-02), +AI skill (LT-03), +статистика/Python/SQL/BI задачи, optional-флаги (LT-06) |
| `data/practiceContent.json` | practice items для всех новых задач (тот же schema) |
| `data/projects.json` | impact-поля (LT-11) |
| `data/career.json` | связь с практикой коммуникаций (LT-04), английский (LT-12) |
| `cases/` + `cases/datasets/` + `cases/index.json` | новые кейсы и датасеты (LT-02, LT-03, LT-05), привязка (LT-09) |

## Порядок выполнения
1. LT-02, LT-03 (закрывают главные рыночные пробелы 2026).
2. LT-04, LT-05, LT-06, LT-07.
3. LT-08…LT-11.
4. LT-12…LT-14.

> Достройка дней Месяцев 2–7 — на стороне владельца плана, вне этого бэклога.
